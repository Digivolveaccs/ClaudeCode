{
  "_comment": [
    "Copy to config/settings.json and fill in. That file is gitignored.",
    "Every value can also come from an environment variable:",
    "INFORMDIRECT_BASE_URL, INFORMDIRECT_API_KEY, and so on.",
    "Prefer environment variables for the key on shared machines.",
    "",
    "CONFIRMED against the live API:",
    "  sandbox     https://sandbox-api.informdirect.co.uk",
    "  production  https://api.informdirect.co.uk",
    "A sandbox key is refused with 401 by the production host, and vice versa -",
    "the host and the key have to match. Swap base_url when you go live.",
    "",
    "Auth is POST {base_url}/authenticate with {\"apiKey\": \"...\"}, which",
    "returns {\"AccessToken\": ..., \"RefreshToken\": ...}. The access token",
    "lasts 15 minutes; /refresh rotates both."
  ],

  "base_url": "https://sandbox-api.informdirect.co.uk",
  "api_key": "",

  "auth_mode": "api_key_token",
  "auth_path": "/authenticate",
  "refresh_path": "/refresh",
  "api_key_field": "apiKey",
  "api_key_in": "body",

  "page_size": 100,
  "timeout": 30,
  "max_retries": 4
}
