{
  "_status": "confirmed",
  "_confirmed_against": "https://sandbox-api.informdirect.co.uk (live probe)",
  "_note": [
    "Paths verified against the live sandbox, not guessed.",
    "GET /companies and GET /companies/{companyNumber} both return the same",
    "envelope: {\"Companies\": [ ... ]}. The company number must be the full",
    "8-character form - an unpadded one is rejected with 400 'The company",
    "number is invalid.'"
  ],
  "_company_record_fields": ["CompanyNumber", "Name", "PublicUrl"],
  "_what_the_api_does_not_return": [
    "company status, accounts year end, accounts filing deadline,",
    "confirmation statement dates, incorporation date, officers,",
    "shareholders, filing history. None of these exist on any endpoint.",
    "The planner's deadline feed CANNOT be built from this API - see the",
    "README. Portfolio membership CAN be."
  ],
  "_not_offered_by_the_api": {
    "_why": [
      "Probed live and confirmed 404: there are no officer, shareholder,",
      "filing or accounts endpoints. The SA close-company check cannot leave",
      "the browser. The client code for them is written and tested, so moving",
      "an entry here into 'operations' is all it would take if they appear."
    ],
    "list_officers": {"method": "GET", "path": "/companies/{company_id}/officers"},
    "list_shareholders": {"method": "GET", "path": "/companies/{company_id}/shareholders"},
    "list_filings": {"method": "GET", "path": "/companies/{company_id}/filings"}
  },
  "_unresolved": {
    "remove_company": [
      "Not located. POST/DELETE on /companies, /companies/{n},",
      "/companies/remove, /companies/delete, /companies/unlink and others all",
      "answer 405 with 'allow: GET'. Probing stopped after the add endpoint",
      "returned 429 ('not meant for bulk uploading'), to avoid the key being",
      "rate-limited further. Ask Inform Direct, or read it off the docs page."
    ]
  },
  "pagination": {
    "style": "auto",
    "page_param": "page",
    "page_size_param": "pageSize",
    "offset_param": "offset",
    "limit_param": "limit",
    "cursor_param": "cursor",
    "first_page": 1,
    "max_pages": 500
  },
  "operations": {
    "list_companies": {
      "method": "GET",
      "path": "/companies",
      "description": "Every company linked to the account. Confirmed."
    },
    "get_company": {
      "method": "GET",
      "path": "/companies/{company_id}",
      "description": "One company by 8-character company number. Confirmed."
    },
    "add_company": {
      "method": "POST",
      "path": "/companies/add",
      "description": "Link one company: {\"CompanyNumber\": \"...\"}. Confirmed. One at a time - bulk payloads are rejected with 429."
    }
  }
}
